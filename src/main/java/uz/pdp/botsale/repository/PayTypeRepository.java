/*
package uz.pdp.botsale.repository;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import uz.pdp.botsale.entity.PayType;

public interface PayTypeRepository extends JpaRepository<PayType,Long> {
   String findByName(String name);
   
   Page<PayType> findAllByNameStartingWithIgnoreCase(String search, Pageable pageableById);
}
*/
